package storagecluster

import (
	"net/http"
)

// DeleteVolume is the handler for DELETE /storagecluster/{label}/volumes/{volumeid}
// Delete Volume
func (api StorageclusterAPI) DeleteVolume(w http.ResponseWriter, r *http.Request) {
}
