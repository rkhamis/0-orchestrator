diff --git a/templates/gateway/actions.py b/templates/gateway/actions.py
index 2a65a3b..10aa25c 100644
--- a/templates/gateway/actions.py
+++ b/templates/gateway/actions.py
@@ -43,6 +43,12 @@ def init(job):
     service = job.service
     containeractor = service.aysrepo.actorGet("container")
     nics = service.model.data.to_dict()['nics']  # get dict version of nics
+    for nic in nics:
+        if 'name' in nic:
+            nic.pop('name')
+        if 'dhcpserver' in nic:
+            nic.pop('dhcpserver')
+
     args = {
         'node': service.model.data.node,
         'flist': 'https://hub.gig.tech/gig-official-apps/g8osgw.flist',
diff --git a/templates/gateway/config.yaml b/templates/gateway/config.yaml
index 3d88e14..5e65a7f 100644
--- a/templates/gateway/config.yaml
+++ b/templates/gateway/config.yaml
@@ -6,7 +6,7 @@ links:
     argname: node
   consume:
     - auto: false
-      min: 1
+      min: 0
       max: 1
       role: container
       argname: container
\ No newline at end of file
